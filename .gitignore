node_modules/
*.backup
*.backup_*.tar.gz
.DS_Store
Thumbs.db
*.log

# NEVER commit TLS private keys or the server ssl dir (lives on the VPS only)
nginx/ssl/
*.key
*.pem

# Working notes — personal, keep LOCAL and out of the public repo
ken-asset-inventory.md
resume-draft.md

# Editor backups
*.bak
public/*.bak
